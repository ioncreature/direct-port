name: Deploy to Stage

on:
  workflow_dispatch:

concurrency:
  group: deploy-stage
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        app:
          - name: api
            dockerfile: apps/api/Dockerfile
          - name: admin-web
            dockerfile: apps/admin-web/Dockerfile
          - name: tg-bot
            dockerfile: apps/tg-bot/Dockerfile
          - name: landing
            dockerfile: apps/landing/Dockerfile
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.app.name }}
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=stage

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.app.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.app.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.app.name }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: stage
    steps:
      - uses: actions/checkout@v4

      - uses: azure/setup-kubectl@v4

      - uses: azure/setup-helm@v4

      - name: Add Helm repositories
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo update

      - name: Build Helm dependencies
        run: helm dependency build ./deploy/helm/directport

      - uses: azure/k8s-set-context@v4
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG_STAGE }}

      - name: Create/Update GHCR pull secret
        run: |
          kubectl -n directport-stage create secret docker-registry ghcr-pull \
            --docker-server=ghcr.io \
            --docker-username='${{ secrets.GHCR_USERNAME }}' \
            --docker-password='${{ secrets.GHCR_PULL_TOKEN }}' \
            --docker-email='devnull@example.com' \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Recover Helm release if pending operation exists
        run: |
          set -euo pipefail

          RELEASE_STATUS="$(helm -n directport-stage status directport-stage -o json 2>/dev/null | jq -r '.info.status' || true)"
          if [[ "$RELEASE_STATUS" =~ ^pending- ]]; then
            echo "Found release in pending state: $RELEASE_STATUS"
            LAST_STABLE_REVISION="$(helm -n directport-stage history directport-stage -o json | jq -r '[.[] | select(.status == "deployed" or .status == "superseded")][-1].revision // empty')"

            if [[ -n "$LAST_STABLE_REVISION" ]]; then
              echo "Rolling back to stable revision: $LAST_STABLE_REVISION"
              helm -n directport-stage rollback directport-stage "$LAST_STABLE_REVISION" --wait --timeout 7m
            else
              echo "No stable revision found; uninstalling broken release to unlock future install"
              helm -n directport-stage uninstall directport-stage || true
            fi
          else
            echo "Release state is '$RELEASE_STATUS' (or release does not exist); no recovery needed"
          fi

      - name: Recreate conflicting pre-existing landing resources for first install
        run: |
          set -euo pipefail

          RELEASE_STATUS="$(helm -n directport-stage status directport-stage -o json 2>/dev/null | jq -r '.info.status' || true)"
          if [[ -n "$RELEASE_STATUS" ]]; then
            echo "Release exists with status '$RELEASE_STATUS'; pre-install recreation is not required"
            exit 0
          fi

          for KIND in service deployment; do
            if kubectl -n directport-stage get "$KIND" directport-stage-landing >/dev/null 2>&1; then
              echo "Deleting pre-existing $KIND/directport-stage-landing to avoid selector/apply conflicts on first Helm install"
              kubectl -n directport-stage delete "$KIND" directport-stage-landing --wait=true
            else
              echo "$KIND/directport-stage-landing does not exist; nothing to recreate"
            fi
          done

      - name: Deploy with Helm
        run: |
          DB_PASSWORD="${{ secrets.STAGE_POSTGRES_PASSWORD }}"
          EXISTING_DB_PASSWORD="$(kubectl -n directport-stage get secret directport-stage-postgresql -o jsonpath='{.data.password}' 2>/dev/null | base64 --decode || true)"
          if [ -n "$EXISTING_DB_PASSWORD" ]; then
            DB_PASSWORD="$EXISTING_DB_PASSWORD"
          fi
          if [ -z "$DB_PASSWORD" ]; then
            echo "WARN: PostgreSQL password is empty and existing secret was not found. Generating a password for first install."
            DB_PASSWORD="$(openssl rand -hex 24)"
          fi

          cat <<EOF > /tmp/helm-secrets.yaml
          secrets:
            jwtSecret: "${{ secrets.JWT_SECRET }}"
            apiInternalKey: "${{ secrets.API_INTERNAL_KEY }}"
            tksApiBaseUrl: "${{ secrets.TKS_API_BASE_URL }}"
            tksTnvedApiKey: "${{ secrets.TKS_TNVED_API_KEY }}"
            tksGoodsApiKey: "${{ secrets.TKS_GOODS_API_KEY }}"
            anthropicApiKey: "${{ secrets.ANTHROPIC_API_KEY }}"
            telegramBotToken: "${{ secrets.TELEGRAM_BOT_TOKEN }}"
          EOF

          helm upgrade --install directport-stage ./deploy/helm/directport \
            -f ./deploy/helm/directport/values-stage.yaml \
            -f /tmp/helm-secrets.yaml \
            --namespace directport-stage \
            --create-namespace \
            --set api.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api \
            --set api.image.tag=${{ github.sha }} \
            --set adminWeb.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-admin-web \
            --set adminWeb.image.tag=${{ github.sha }} \
            --set tgBot.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-tg-bot \
            --set tgBot.image.tag=${{ github.sha }} \
            --set landing.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-landing \
            --set landing.image.tag=${{ github.sha }} \
            --set postgresql.auth.password="$DB_PASSWORD" \
            --set global.postgresql.auth.password="$DB_PASSWORD" \
            --wait --timeout 7m

          rm -f /tmp/helm-secrets.yaml
