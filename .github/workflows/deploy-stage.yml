name: Deploy to Stage

on:
  workflow_dispatch:

concurrency:
  group: deploy-stage
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        app:
          - name: api
            dockerfile: apps/api/Dockerfile
          - name: admin-web
            dockerfile: apps/admin-web/Dockerfile
          - name: client-bot
            dockerfile: apps/client-bot/Dockerfile
          - name: manager-bot
            dockerfile: apps/manager-bot/Dockerfile
          - name: landing
            dockerfile: apps/landing/Dockerfile
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/metadata-action@v6
        id: meta
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.app.name }}
          tags: |
            type=sha,prefix=,format=long
            type=raw,value=stage

      - uses: docker/build-push-action@v7
        with:
          context: .
          file: ${{ matrix.app.dockerfile }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.app.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.app.name }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    environment: stage
    steps:
      - uses: actions/checkout@v6
      - uses: azure/setup-kubectl@v5
      - uses: azure/setup-helm@v5

      - name: Build Helm dependencies
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm dependency build ./deploy/helm/directport

      - uses: azure/k8s-set-context@v5
        with:
          kubeconfig: ${{ secrets.KUBE_CONFIG_STAGE }}

      - name: Ensure GHCR pull secret
        run: |
          kubectl -n directport-stage create secret docker-registry ghcr-pull \
            --docker-server=ghcr.io \
            --docker-username='${{ secrets.GHCR_USERNAME }}' \
            --docker-password='${{ secrets.GHCR_PULL_TOKEN }}' \
            --dry-run=client -o yaml | kubectl apply -f -

      - name: Deploy with Helm
        env:
          MAX_PRODUCT_ROWS_PER_FILE: ${{ vars.MAX_PRODUCT_ROWS_PER_FILE }}
        run: |
          set -euo pipefail

          # Reuse existing Postgres password if the secret already exists
          # (иначе Bitnami перегенерирует пароль и PVC сломается)
          DB_PASSWORD="$(kubectl -n directport-stage get secret directport-stage-postgresql \
            -o jsonpath='{.data.password}' 2>/dev/null | base64 -d || true)"
          DB_PASSWORD="${DB_PASSWORD:-${{ secrets.STAGE_POSTGRES_PASSWORD }}}"

          cat <<EOF > /tmp/helm-secrets.yaml
          secrets:
            jwtSecret: "${{ secrets.JWT_SECRET }}"
            apiInternalKey: "${{ secrets.API_INTERNAL_KEY }}"
            tksApiBaseUrl: "${{ secrets.TKS_API_BASE_URL }}"
            tksTnvedApiKey: "${{ secrets.TKS_TNVED_API_KEY }}"
            tksGoodsApiKey: "${{ secrets.TKS_GOODS_API_KEY }}"
            anthropicApiKey: "${{ secrets.ANTHROPIC_API_KEY }}"
            clientBotTelegramToken: "${{ secrets.CLIENT_BOT_TELEGRAM_TOKEN }}"
            managerBotTelegramToken: "${{ secrets.MANAGER_BOT_TELEGRAM_TOKEN }}"
            managerBotUsername: "${{ secrets.MANAGER_BOT_USERNAME }}"
          EOF

          # Если переменная не задана в GitHub Environment variables — используем дефолт из values.yaml.
          EXTRA_HELM_ARGS=()
          if [ -n "${MAX_PRODUCT_ROWS_PER_FILE:-}" ]; then
            EXTRA_HELM_ARGS+=(--set-string "api.env.MAX_PRODUCT_ROWS_PER_FILE=${MAX_PRODUCT_ROWS_PER_FILE}")
          fi

          helm upgrade --install directport-stage ./deploy/helm/directport \
            --namespace directport-stage \
            --create-namespace \
            -f ./deploy/helm/directport/values-stage.yaml \
            -f /tmp/helm-secrets.yaml \
            --set api.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-api \
            --set api.image.tag=${{ github.sha }} \
            --set adminWeb.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-admin-web \
            --set adminWeb.image.tag=${{ github.sha }} \
            --set clientBot.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-client-bot \
            --set clientBot.image.tag=${{ github.sha }} \
            --set managerBot.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-manager-bot \
            --set managerBot.image.tag=${{ github.sha }} \
            --set landing.image.repository=${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-landing \
            --set landing.image.tag=${{ github.sha }} \
            --set postgresql.auth.password="$DB_PASSWORD" \
            --set global.postgresql.auth.password="$DB_PASSWORD" \
            "${EXTRA_HELM_ARGS[@]}" \
            --wait --timeout 7m

          rm -f /tmp/helm-secrets.yaml
