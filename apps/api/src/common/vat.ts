/**
 * Стандартная ставка НДС в РФ (%). Применяется как дефолт, когда TKS не вернул
 * `NDS` для кода (детерминистический fallback-расчёт и UI-справочник ТН ВЭД).
 * С 2026 г. стандартная ставка — 22%.
 *
 * Основной путь расчёта берёт ставку из TKS (`rates.NDS`); этот дефолт срабатывает
 * только при её отсутствии. Раньше значение было захардкожено (20) в нескольких
 * местах — теперь единая константа.
 */
export const DEFAULT_VAT_RATE = 22;
