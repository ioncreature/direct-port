apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "directport.fullname" . }}-client-bot
  labels:
    {{- include "directport.labels" . | nindent 4 }}
    app.kubernetes.io/component: client-bot
spec:
  replicas: {{ .Values.clientBot.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "directport.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: client-bot
  template:
    metadata:
      labels:
        {{- include "directport.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: client-bot
    spec:
      {{- $redisHost := ternary (printf "%s-redis-master" .Release.Name) .Values.redis.host .Values.redis.enabled }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: wait-for-redis
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z {{ $redisHost }} {{ .Values.redis.port }}; do echo waiting for redis; sleep 2; done"]
      containers:
        - name: client-bot
          image: "{{ .Values.clientBot.image.repository }}:{{ .Values.clientBot.image.tag }}"
          imagePullPolicy: {{ .Values.clientBot.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.clientBot.port }}
              protocol: TCP
          envFrom:
            - secretRef:
                name: {{ include "directport.secretName" . }}
          env:
            - name: BOT_PORT
              value: {{ .Values.clientBot.port | quote }}
            - name: API_BASE_URL
              value: {{ include "directport.apiUrl" . | quote }}
            # Дефолтная компания (000…001) НЕ поднимает бота из env TELEGRAM_BOT_TOKEN:
            # боты принадлежат компаниям-тенантам и грузятся из GET /internal/bots. Иначе токен
            # дублировался бы с токеном компании-владельца → getUpdates 409 Conflict → CrashLoop.
            # Токен client-бота (CLIENT_BOT_TELEGRAM_TOKEN) остаётся в секрете — его читает api
            # для верификации подписи Telegram Login Widget дефолтной компании.
          livenessProbe:
            tcpSocket:
              port: {{ .Values.clientBot.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: {{ .Values.clientBot.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.clientBot.resources | nindent 12 }}
